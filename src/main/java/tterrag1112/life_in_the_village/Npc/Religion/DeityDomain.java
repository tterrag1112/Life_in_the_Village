package tterrag1112.life_in_the_village.Npc.Religion;

/**
 * The broad domain of a {@link God} — its sphere, which the divine-event content
 * tables ({@code Miracles}/{@code Curses}) are keyed by and which drives the
 * manifestation/voice flavour. (F1a cleanup — relocated from {@code ReligionIdentity}
 * to the religion package as a god concept; the Loom's impersonal Pattern has the
 * {@code FATE} domain.)
 */
public enum DeityDomain { SUN, SEA, FORGE, FATE }
