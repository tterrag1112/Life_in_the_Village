package tterrag1112.life_in_the_village.Village.Planning.V2.Culture;

/**
 * V2 culture plaza shape preference. Distinct from V1's
 * {@code Decoration.Plaza.PlazaShape} — V2 treats plaza shape as a
 * culture-level bias, while the V1 enum enumerates renderer
 * variants. They may converge later.
 */
public enum PlazaShape {
    CIRCLE,
    SQUARE,
    IRREGULAR
}
