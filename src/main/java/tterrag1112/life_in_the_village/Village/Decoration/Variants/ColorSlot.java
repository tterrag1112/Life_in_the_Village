package tterrag1112.life_in_the_village.Village.Decoration.Variants;

/**
 * Doc 15 — three color channels. The post-stamp tint pass tints
 * {@code white_*} marker blocks with the building's chosen dye for
 * each declared slot; slots not listed in a variant's
 * {@code colorSlots} are skipped.
 */
public enum ColorSlot {
    PRIMARY, ACCENT, ROOF
}
